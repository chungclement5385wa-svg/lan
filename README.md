# 汕尾霖墅装饰设计工程有限公司 - 页面说明

此页面为公司展示页模版，已生成 `index.html`，请按下列说明准备静态资源并预览：

必备文件（放在与 `index.html` 同一目录下）：

- `logo.png`      - 公司 logo（可选，若无则显示文本公司名）
- `1.jpg` ~ `5.jpg` - 首页图（按序从左到右展示）

可选（用于 CAD / 效果图示例，占位说明）：

- `cad1.jpg`, `cad2.jpg` - CAD 平面图示例（可选）
- `render1.jpg`, `render2.jpg` - 效果图示例（可选）

提醒：如果你没有真实图片，可以先放置占位图片，文件名请与上面一致以保证页面能正确显示。

本地预览（Windows PowerShell）：

```powershell
# 进入项目目录
cd "e:\vs工作"
# 启动一个简单的 HTTP 服务（需已安装 Python）
python -m http.server 8000
# 打开浏览器访问 http://localhost:8000
```

替换联系方式：

- 打开 `index.html`，在“联系我们”部分将 `+86-138-0000-0000` 替换为公司的真实联系电话。

后续建议：

- 若需要将表单连接到邮件或后台，请告诉我你希望的接收方式（SMTP、第三方 API 或静态表单服务），我可以帮你接入。
- 若你希望我把页面做得更专业（企业色系、更多案例页、移动端交互），告诉我设计偏好，我会继续迭代。